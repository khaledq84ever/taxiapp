[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/bin/sh -c 'npx prisma db push --accept-data-loss && node dist/src/main'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
